import React, { Component } from 'react';

class Header extends Component {
    render() {
        return (
            <div className >
                Find Hostels in Popular Cities
            </div>
        );
    }
}

export default Header;